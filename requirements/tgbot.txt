# Telegram bot dependencies — a component of the prod venv (and dev, for parity).
# Composed into prod.txt and dev.txt; not installed standalone.
python-telegram-bot[job-queue]>=22.0
anthropic>=0.40.0
openai>=1.0.0
python-dotenv>=1.0.0
